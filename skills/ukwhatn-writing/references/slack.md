# Slack の書き分け

Slackは相手と用途で文体が最も大きく変わる。**雑談（砕け）**・**対外・公的・目上（完全敬語）**・**業務の依頼・確認・報告（端的）**の3レジスタを使い分ける。同じ同僚相手でも、雑談ならA、業務の依頼・確認ならCになる（相手だけでなく用途で選ぶ）。

---

## レジスタ A: 雑談（自分のtimes / 同僚との雑談）（砕け）

タメ口・常体＋終助詞。句点をほぼ打たず、改行や全角スペース `　` で区切る。1メッセージ＝1〜2行で連投。

### 特徴
- 文末: `〜な` `〜わ` `〜よ` `〜か`（`あったわ` `出るな` `戻るか`）
- ぼかし: `〜かも` `〜と思う` `〜気がする`
- `（）` で自嘲・オチ・本音補足
- 三点リーダ引き伸ばし `......` `‥…………` で脱力
- カスタム絵文字、`:emoji: < セリフ` 吹き出しネタ
- フィラー `あと、` `たぶん` `一応` `なんか` `べつに` `たしかに`

### 実例（原文ママ）
- `ご飯食べる暇がない〜`
- `なんか知らんうちにClaude Codeがいい感じになってるな　Zedから戻るか`
- `worktree、claudeにいじってもらうことしかしなくなった`
- `大変なんすよ……（）`
- `たしかに..........カスタム性というか載ってるOSかな........ならNASか.................`
- `べつにClaudeもCodexも契約したら良いんじゃないですか（）`
- `:ukwhatn_oni: < ルールを守っていない悪い子はいねが〜`

### 後輩へのコーチング（砕けつつ、答えでなく問いを投げる）
```
現状の深堀り自体はだいぶできててあんまり言うこと無いな（）

あと、「積極的にリードしない」と「介入せずに放置する」はイコールではないと思っていて、軌道修正はするけど意思決定はしない立ち回りの形を模索できるといいかもしれない（例えば ズレてると思ったら、答えは出さず「いまここズレてない？」って問いだけ投げる みたいな）
```
→ 指示でなく提案（`〜してみるのがおすすめです` `〜できるといいかもしれない`）。断定しない。

---

## レジスタ B: 対外・公的・目上（社外/全社/事務局/上長）（完全敬語）

ビジネス敬語。**「お疲れ様です。」で開始 → 文脈1〜2文 → 「〜でしょうか。」で問う → 「よろしくお願いいたします」で締め**。依頼・お詫びには `:bow-nya:` `:pray-nya:` `:man-bowing:` を1個クッションとして添える。

### 構成: 文脈 → 問い（結論先行ではなく、背景を置いてから用件）

### 質問・確認の定型
- 「〜でしょうか。」「〜という認識／理解で良いでしょうか。」「〜してもよろしいでしょうか。」
- 許可は必ず可否を問う形にする。自分の理解を確認して合意を取る。

### 実例（骨格）
```
<@担当者> san
お疲れ様です。
<対象環境>において<条件>の場合、<期待した挙動>が利用できないようなのですが、こちらは意図した制限でしょうか。
```
```
お疲れ様です、<対象>を対象として参加させていただきたいのですが、一点（念の為）確認させてください。
```
- 依頼＋配慮: `こちら、何か操作方法が間違っていればご教示いただきたく、お手隙でご確認いただけますと幸いです :man-bowing:`
- 依頼の締め: `お手数おかけして申し訳ありません、よろしくお願いいたします :bow-nya:`
- お礼: `承知いたしました、ありがとうございます :pray-nya:` / `素早いご対応ありがとうございました！！`

### 配慮表現（的確に、ただし要件は曖昧にしない）
「お手数ですが」「お手隙で結構です」「念の為」「（メンション失礼します）」を使う。配慮しつつ、可否・対象・期限は具体で書く。根拠リンク（Wiki/Notion/Slackパーマリンク/GitHub）を併記する。

### 込み入った報告は構造化
背景を1文置いてから、箇条書きと `=>` で「期待値 vs 実測」を対比する。箇条書きは `- ` で書き、`•` を文字として打たない（描画される経路と崩れる経路は「MCP 経由で投稿するときの書式」）。
```
一応事象の詳細
- プロジェクトコード一覧で〜 => 出る
- 週次入力画面〜 => 出ない
```

---

## レジスタ C: 業務の依頼・確認・報告（後輩/対等な実務相手）（端的）

立場が上・気心知れた実務相手への業務連絡には、敬語を薄くして端的に詰める。絵文字少なめ。同僚相手でも雑談はA・業務連絡はCと用途で分ける。

### 実例（原文ママ）
- 催促: `本件何らかアップデートありますか？`
- 引き継ぎ依頼（砕け）: `あのMTGマジで1回も出たことないので雑に引き継ぎをいただきたく.........................`
- 判断の共有（口語）: `既に離任されているようですが、<担当者>から繋げてもらうのが丸いという結論でした`
- 回答・注意喚起: `postman、アカウント作ると強制的にエンドポイントやmessage bodyの情報がクラウド同期されるので、そこだけ注意必要だと思います`
- 前提の問い返し: `「geminiを利用できる状態」=「GeminiシリーズのAPIを叩ける状態」ですか？`

---

## 共通の注意

- **「業務だから一律丁寧」ではない**。相手と立場で B（社外・目上）と C（部下・対等）を切り替える。
- 質問への回答では、まず相手の前提を `=` で言い換え確認してから答えることがある（認識ズレ防止）。
- 長い三点リーダ `.....` はレジスタBでもトーンダウンに稀に出る。
- 締めの定型（よろしくお願いいたします）はレジスタBのみ。内輪では締めない or 軽く。
- `（）`・三点リーダ・全角スペース・フィラーは「らしさ」を出すが、**読みやすさを落とすので使いすぎない**（1メッセージに1〜2個まで。レジスタA中心、B/Cでは原則使わない）。
- **1メッセージの中でレジスタを混ぜない**。砕けた語り口（タメ口・「おまたせ」・「〜でどうでしょう」）と構造化装飾（太字見出し・表・番号付き節）を同居させると芝居がかった文になる。業務の報告（レジスタC）は敬体を保ったまま事実を並べ、装飾は箇条書きまでにする。

---

## MCP 経由で投稿するときの書式

**目的: 箇条書き・強調・コード・引用が、受け手の画面でネイティブの書式として描画される状態で届ける。** 記号を文字として打った箇条書き（`•` `◦` の直書き）や、変換されずに文字へ落ちた箇条書きは、階層の無い平坦なテキストになる。

Slack MCP の送信ツール（`slack_send_message`）と下書き作成ツール（`slack_send_message_draft`）はどちらも `blocks` を受け付けず、`message`（markdown 文字列）だけを取るが、**保存される書式が経路で異なる**:

| 経路 | 書式の扱い | 乗せてよい文 |
|---|---|---|
| 送信ツール | 標準 markdown をネイティブ書式に変換する（`- ` とインデントの箇条書き、`_斜体_`、`` `code` ``、コードブロック、引用、リンク） | 構造を持つ文を含む全て |
| 下書き作成ツール | レガシーのプレーンテキスト書式で保存する。強調は変換されるが、**箇条書きは文字の `•` に落ちる** | 段落だけの文（箇条書き・見出し・表を含まないもの） |

- 箇条書きは `- ` とインデントで書き、`•` `◦` を文字として打たない（送信ツールが変換するのはこの形）
- ユーザーグループのメンションは `<!subteam^グループID>` 形式で書く
- ツールの変換挙動は版で変わる。初めて使う経路・ツールの版が変わった後は、構造を持つ文を検証先に1度送って画面で描画を見る（ドキュメントの記載は実機の挙動を保証しない。AGENTS.md「外部API・ランタイム/プラットフォームAPI」と同じ扱い）

### 承認が要る投稿（対外・公的なもの）の経路

内容の承認と描画の実物確認を両方満たす経路を、文の形で選ぶ:

- **段落だけの文**: 下書き作成ツールを使う。チャンネルの下書き欄に入り、本人が画面で確認・編集して自分で送れる
- **構造を持つ文（箇条書き・見出し・表）**: 下書き経路に乗せない（構造が崩れた状態のものを承認することになる）。次の順で進める
  1. 本文をチャットで提示する（各文の出所を添える。AGENTS.md「最優先指示」）
  2. 送信ツールで検証先へ投稿し、本人が画面で描画と内容を確認する。崩れていれば直して再投稿する。検証先は自分宛 DM でよい（送信ツールの宛先に自分の user_id を渡せる）。使えないワークスペースでは本人だけが見る場所を1つ決めておく
  3. 確認が取れてから、本番の宛先へ送信ツールで投稿する。本番への投稿は名指しの承認を受けてから行う（AGENTS.md「ユーザーへの質問」の外部作用の承認）
